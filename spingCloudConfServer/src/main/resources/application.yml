server:
  port: 7000
#服务名字
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
#git 仓库的地址
          uri: https://github.com/zhouchun-111/test
#git 仓库的账号密码
          username: zhouchun-111
          password: zhouchun920507
#加入注册中心，实现高可用
eureka:
  client:
    service-url:
       defaultZone: http://localhost:9999/eureka/
                                  